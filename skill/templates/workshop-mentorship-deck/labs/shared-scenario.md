# Cenário compartilhado — `<Curso>`

Cenário base reaproveitado pelos labs de todos os dias.

## Contexto

`<descrição do ambiente, app, repo, queries, runtime>`

## Pré-requisitos comuns

- `<dep 1>`
- `<dep 2>`

## Identidade

- `service.name`: `<valor>`
- `service.namespace`: `<valor>`
- `deployment.environment`: `<valor>`
- OTLP endpoint: `<valor>`

## Comandos de saneamento

```bash
# valida que ambiente está pronto
<comando>

# tear-down
<comando>
```
