# Lab — Dia 1: `<Título>`

## O que se prova

`<frase de 1-2 linhas>`

## Pré-requisitos

- `<dep 1>`
- `<dep 2>`
- ambiente base do [shared-scenario](../shared-scenario.md)

## Execução

### Passo 1 — `<ação>`

```bash
<comando>
```

Resultado esperado:

```
<output esperado>
```

### Passo 2 — `<ação>`

```bash
<comando>
```

### Passo 3 — Validação

```bash
<comando de validação>
```

## Sucesso

`<o que precisa estar visível pra dizer que rodou>`:

- `<sinal 1>`
- `<sinal 2>`

## Falhas esperadas

> **Atenção:** os erros abaixo são esperados em alguns ambientes. Saiba reconhecer.

- **Falha A** — sintoma `<X>`, causa `<Y>`, fix `<Z>`
- **Falha B** — sintoma `<X>`, causa `<Y>`, fix `<Z>`

## Debrief

Perguntas pra discutir com a turma:

- O que ficou mais visível depois desse lab?
- Em que ponto o "automágico" deixou de ajudar?
- O que se aprende sobre o sistema só depois de quebrar?
